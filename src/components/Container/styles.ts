import styled from "styled-components"

export const Container = styled.main`
    display: flex;
    justify-content: space-between;
    width: ${1500 * 0.063}rem;
    margin: 0 auto;
    padding: ${25 * 0.063}rem ${12 * 0.063}rem
`