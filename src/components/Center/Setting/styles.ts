import styled from "styled-components"

export const SettingHeading = styled.div`
    width: ${690 * 0.063}rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: ${24 * 0.063}rem ${38 * 0.063}rem ${24 * 0.063}rem ${34 * 0.063}rem;
    background: #fff;
    border-radius: ${20 * 0.063}rem;
    margin: 0 auto ${10 * 0.063}rem;

`
export const Text = styled.p`
    font-size: ${14 * 0.063}rem;
    line-height: ${17 * 0.063}rem;
`